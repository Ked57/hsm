version: 2.1
executors:
  main-executor:
    docker:
      - image: circleci/node:lts
  docker-publisher:
    environment:
        IMAGE_NAME: jonathancardoso/building-on-ci
    docker:
      - image: circleci/buildpack-deps:stretch

jobs:
  install-dependencies:
    executor: main-executor
    parameters:
      app-path:
        type: string
    steps:
      - checkout
      - run: cd << parameters.app-path >> && npm ci
      - persist_to_workspace:
          root: ./
          paths:
            - << parameters.app-path >>/node_modules
            - ~/.cache

  check-format:
    executor: main-executor
    parameters:
        app-path:
          type: string
    steps:
      - checkout
      - attach_workspace:
          at: ./
      - run: cd << parameters.app-path >> && npm run format:check

  test:
    executor: main-executor
    parameters:
        app-path:
          type: string
    steps:
      - checkout
      - attach_workspace:
          at: ./
      - run: cd << parameters.app-path >> && npm run test

  build:
    executor: main-executor
    parameters:
        app-path:
          type: string
    steps:
      - checkout
      - attach_workspace:
          at: ./
      - run: cd << parameters.app-path >> && npm run build

workflows:
  version: 2

  deploy:
    jobs:
      - install-dependencies:
          name: install-dependencies-serverapp
          app-path: "serverapp"
          filters:
            tags:
              only: /.*/
      - install-dependencies:
          name: install-dependencies-webapp
          app-path: "webapp"
          filters:
            tags:
              only: /.*/
      - check-format:
          name: check-format-serverapp
          app-path: "serverapp"
          requires:
            - install-dependencies-serverapp
          filters:
            tags:
              only: /.*/
      - check-format:
          name: check-format-webapp
          app-path: "webapp"
          requires:
            - install-dependencies-webapp
          filters:
            tags:
              only: /.*/
      - test:
          name: test-serverapp
          app-path: "serverapp"
          requires:
            - check-format-serverapp
          filters:
            tags:
              only: /.*/
      - test:
          name: test-webapp
          app-path: "webapp"
          requires:
            - check-format-webapp
          filters:
            tags:
              only: /.*/
      - build:
          name: build-serverapp
          app-path: "serverapp"
          requires:
            - test-serverapp
          filters:
            tags:
              only: /.*/
      - build:
          name: build-webapp
          app-path: "webapp"
          requires:
            - test-webapp
          filters:
            tags:
              only: /.*/